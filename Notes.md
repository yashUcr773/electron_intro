<!-- Debug main using chrome dev tools. -->
1. Run the command `npm run debug:main`
2. open chrome.
3. go to url `chrome://inspect/#devices`
4. note: you may need to add `localhost:port` in configure and enable port forwarding
5. inspect the app